# find-similar

Find events similar to a saved event using embedding-based similarity.

## Usage

```
/find-similar --event-id <id> [--top <n>]
```

## What to do

1. Run `python main.py similar --event-id $ID --top $N`
2. Display the top matches with their similarity scores
3. Offer to draft outreach emails for any of the matches
